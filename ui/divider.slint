import { Palette } from "std-widgets.slint";

export component HDivider inherits Rectangle {
    height: 1px;
    background: Palette.border.with-alpha(0.1);
}

export component VDivider inherits Rectangle {
    width: 1px;
    background: Palette.border.with-alpha(0.1);
}
